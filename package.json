{
  "name": "home-control",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "run-p dev:*",
    "dev:client": "npm run dev -w packages/client",
    "dev:server": "npm run dev -w packages/server",
    "start": "npm run start -w packages/server",
    "build": "npm run lint && npm run build -w packages/client",
    "lint": "tsc"
  },
  "devDependencies": {
    "@types/node": "^20.8.0",
    "eslint": "^8.50.0",
    "eslint-config-react-app": "^7.0.1",
    "npm-run-all": "^4.1.5",
    "tsx": "^3.12.3",
    "typescript": "^4.9.3"
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "workspaces": [
    "packages/jdownloader-api",
    "packages/server",
    "packages/client",
    "packages/types"
  ]
}
